apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: valkey-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: valkey-dev
    server: https://kubernetes.default.svc
  project: default
  source:
    path: apps/valkey/overlays/dev
    repoURL: https://github.com/vatusa/gitops
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
