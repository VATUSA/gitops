apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: valkey-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: valkey-prod
    server: https://kubernetes.default.svc
  project: default
  source:
    path: apps/valkey/overlays/prod
    repoURL: https://github.com/vatusa/gitops
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
