apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: schedule-message-bot
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: schedule-message-bot
    server: https://kubernetes.default.svc
  project: default
  source:
    path: schedule-message-bot/overlays/prod
    repoURL: https://github.com/vatusa/gitops
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
