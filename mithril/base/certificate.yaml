apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mithril-tls
spec:
  dnsNames:
    - api.vatusa.net
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: mithril-tls
