apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base
- namespace.yaml
- certificate.yaml
- portal-ingress.yaml
- staff-ingress.yaml
- configmap.yaml
- portal-pdb.yaml
- staff-pdb.yaml
images:
# NOTE: these tags are placeholders only, set so `kustomize build` succeeds
# for local review. The real prod tag is set by the prod-release.yml ->
# common-release.yml workflow (`kustomize edit set image`) when this
# overlay is actually released — do not treat these as the deploy tag.
- name: vatusa/portal
  newName: vatusa/portal
  newTag: f48104cb1af85d25429d00a1aa7bc054f1b00745
- name: vatusa/staff
  newName: vatusa/staff
  newTag: f48104cb1af85d25429d00a1aa7bc054f1b00745
