apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: next-vatusa-net-tls
spec:
  dnsNames:
    - next.vatusa.net
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: next-vatusa-net-tls
