apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mithril-tls
spec:
  dnsNames:
    - mithril.vatusa.net
    - vatusa.net
    - www.vatusa.net
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: mithril-tls
