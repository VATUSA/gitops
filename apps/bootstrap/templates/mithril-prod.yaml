apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mithril-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: mithril-prod
    server: https://kubernetes.default.svc
  project: default
  source:
    path: mithril/overlays/prod
    repoURL: https://github.com/vatusa/gitops
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
