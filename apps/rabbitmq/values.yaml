rabbitmq:
  auth:
    username: admin
    existingPasswordSecret: passwords
  primary:
    resources:
      requests:
        memory: 1Gi
        cpu: 1
      limits:
        memory: 1.5Gi
        cpu: 1500m