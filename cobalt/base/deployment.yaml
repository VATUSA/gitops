apiVersion: apps/v1
kind: Deployment
metadata:
  name: cobalt
  labels:
    app: cobalt
spec:
  replicas: 2
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: cobalt
  template:
    metadata:
      name: cobalt
      labels:
        app: cobalt
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - cobalt
                topologyKey: kubernetes.io/hostname
      containers:
        - name: cobalt
          image: vatusa/cobalt
          imagePullPolicy: Always
          # Go HTTP API idling near-zero (~1m CPU, ~8-30Mi RSS). Requests sized
          # to ~2x the observed working set for GC-heap/request headroom; the
          # 512Mi limit absorbs spikes.
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 512Mi
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 8080
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 1
            successThreshold: 1
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: 8080
              scheme: HTTP
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 1
            successThreshold: 1
            failureThreshold: 3
          envFrom:
            - secretRef:
                name: cobalt-secrets
            - configMapRef:
                name: cobalt-config
      serviceAccountName: cobalt-sa
      restartPolicy: Always
